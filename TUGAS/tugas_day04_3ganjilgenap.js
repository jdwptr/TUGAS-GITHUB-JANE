let y = `Nomor Ganjil dan Genap `
for (let x = 0; x <= 25; x++) {
    if (x % 2 === 0) {
        console.log(`NOMOR URUT GENAP ${x}`)
    } else {
        console.log(`NOMOR URUT GANJIL ${x}`)
    }
}